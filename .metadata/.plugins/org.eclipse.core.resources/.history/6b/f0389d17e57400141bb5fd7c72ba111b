package edu.dom.kamil.selfstudy;

public class Czas {
	
	private int hour;
	private int minutes;
	private int seconds;
	
	public void setTime(int h, int m, int s) {
		hour = ((h>=0 && h <24) ? h : 0);
		minutes = ((h>=0 && h <24) ? h : 0);
		seconds = ((h>=0 && h <24) ? h : 0);
	}
	
	
}
